# Slides Track

Put slide source files here unless the project already uses a different structure.

If the project uses Quarto slides with the Caltech theme, install `caltech-revealjs` in the consuming project and record the deck path plus render command in `workflow/project.yml`.
